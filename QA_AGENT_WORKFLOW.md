# 実務Q&A ＆ アップデート速報 運用・エージェント連携ワークフロー

本ドキュメントは、「上善如水 壁量計算WEB」の紹介ポータル（`sns.eie.tokyo`）における、
**ユーザー質疑・不具合報告の受付 ➔ 運営通知 ➔ エージェント（Antigravity）によるスクショ・動画キャプチャ ➔ Q&A・SNS公開** の自動・半自動連携手順を定めた運用ガイドです。

---

## 1. 質疑受付 ＆ 添付ファイル・メール通知の仕組み

### ユーザー側の送信フロー
1. ユーザーが `https://sns.eie.tokyo#qa` の「実務質問・不具合報告フォーム」に入力。
2. **検証用ファイルの添付（任意・推奨）**：
   - 読み込ませようとしているDXFファイル（`.dxf`）、作成中プロジェクトデータ（`.json`）、エラー画面スクショ（`.png`/`.jpg`）、ZIP等をドラッグ＆ドロップ（最大3ファイル・合計20MBまで）。
3. **【最重要】事前承諾免責バナー**：
   - 「個人情報・特定物件情報を伏せた上で、事前の個別承諾なくQ&Aや公式SNSに掲載する旨」に同意チェック。
4. 送信ボタンを押すと、サーバー側API（`api/submit_ticket.php`）へ非同期POST送信。

### バックエンドAPI（`api/submit_ticket.php`）の処理
- チケットID（例: `ticket_20260920_161520_a8f9`）を自動採番。
- ファイルをセキュアに保存（`uploads/tickets/{ticket_id}/`）。
- チケット情報（`ticket.json`）を生成。
- 管理者（**`support@eie.tokyo`**）宛てにチケット詳細とファイル一覧を即時メール通知。

---

## 2. エージェントによる「添付ファイル自動読込 ＆ 修正計画（Implementation Plan）生成」パイプライン

ユーザーからファイル添付付きの報告が届いた際、エージェントはファイルを自動取得・診断し、管理者へ修正計画書を提示します。

### 実行手順（コマンド1発で完了）
```bash
# 最新チケットを自動取得して診断する場合
node sns_portal/scripts/inspect_ticket.js --latest

# 特定のチケットIDを指定する場合
node sns_portal/scripts/inspect_ticket.js <ticket_id>
```

### スクリプトの自動処理内容
1. **自動ダウンロード**: SSH/SCP経由でXServerから添付ファイル一式をローカル作業ディレクトリ（`sns_portal/scratch/tickets/{ticket_id}/`）へ取得。
2. **DXF構文・互換性の自動診断**:
   - `dxf-parser` による構文パース
   - AutoCADバージョン、レイヤ一覧（通り芯・壁芯レイヤ）の判定
   - 外接矩形寸法・原点座標（0,0からの乖離・用紙外配置の有無）の検出
   - 特殊エンティティ（SPLINE, ELLIPSE, 複合ポリライン）やブロック（INSERT）の集計
3. **プロジェクトJSONの整合性検証**:
   - 柱（pillars）、壁（walls）、通り芯（grids）の必須キー欠損やデータ破損を自動検出。
4. **修正計画書（Implementation Plan）の自動生成**:
   - `implementation_plan_{ticketId}.md` を自動生成し、管理者へ提示。
   - 【事象サマリー】➔【診断結果】➔【原因特定】➔【プログラム修正案】➔【ユーザー回答案】➔【Q&A・更新速報案】が一気通貫で出力されます。

---

## 2. エージェントによる「スクショ・操作動画付き回答」返答フロー

ユーザーからの質疑に対し、運営者様が回答の要点や修正内容をチャットで伝えるだけで、
**AIエージェント（Antigravity）が実機画面を自動操作してスクショ・動画を生成し、Q&A・更新ログへ自動反映**します。

### ステップ①：運営者様からエージェントへの指示
ユーザーからの質問が届いたら、Antigravityチャットに以下のように指示してください：
> **（指示の例）**  
> 「ユーザーから『ベタ基礎の人通口開口補強の入れ方がわからない』と質問が来ました。  
> 回答方針：基礎伏図の人通口アイコンをクリックして開口幅600mmを指定すれば自動補強されます。  
> この内容を汲んで、操作画面のスクショ付きでQ&Aに追加してください。」

### ステップ②：エージェントの自動実行（スクショ・動画キャプチャ）
1. エージェントがブラウザツール（`browser_subagent`）を起動し、ローカルの `wall_4split_v2`（操作画面）を開きます。
2. 指示された操作手順（例: 基礎伏図タブを開き、人通口をクリックして配置する動作）を自動実行。
3. 該当の操作画面を高解像度スクリーンショット（または操作動画WebP/GIF）としてキャプチャし、`sns_portal/assets/qa/` に保存します。

### ステップ③：Q&A・更新データベースへの自動追記
エージェントが `sns_portal/assets/js/qa_data.js` を更新します：
- **実務質問の場合**：`QA_DATABASE` に質問、丁寧な構造解説、キャプチャしたスクショ画像/動画、タグを自動追加。
- **不具合解消の場合**：`UPDATE_LOGS` に「⚡ 即日解消」バッジ、報告者、修正内容、対応バージョンを自動追加。

### ステップ④：本番サーバーへの即時デプロイ同期【必須・厳格義務】
エージェントは `qa_data.js` やアセットの追加後、**直ちに以下の同期コマンドを実行して本番環境（`https://sns.eie.tokyo`）へ反映させます**：
```bash
git add .
git commit -m "Add QA & update log: <質問または解消内容>"
git push origin main
ssh -o BatchMode=yes -p 10022 mdo3@mdo3.xsrv.jp "cd eie.tokyo/public_html/sns && git pull origin main"
```

### ステップ⑤：SNS（X / note+ / Instagram）への爆速展開
ポータル上の各更新カードには **「𝕏 ポスト文をコピー」「📝 note・SNS用要約コピー」** ボタンが設置されているため：
1. 運営者様がボタンを1クリック ➔ クリップボードに投稿用テキストがコピー。
2. X（旧Twitter）やnote+にそのまま貼り付けて投稿するだけで、**「不具合報告からわずか数時間で解消した圧倒的な開発スピード」**を即座にSNSでアピールできます。

---

## 3. Q&Aデータ構造（`qa_data.js`）の仕様

```javascript
// Q&Aアイテムの基本スキーマ
{
  id: "qa-005",
  category: "foundation",           // 'dxf' | 'wall4' | 'nvalue' | 'foundation' | 'report' | 'other'
  categoryLabel: "ベタ基礎・人通口",
  title: "質問タイトル",
  userType: "設計事務所 実務者",
  date: "2026-09-20",
  status: "解決済",
  question: "ユーザーからのリアルな質問本文",
  answerText: "エージェント・運営による解説（**太字強調**対応）",
  mediaType: "image",              // 'image' または 'video'
  mediaThumbnail: "assets/qa/capture_foundation_opening.png", // 生成スクショ
  videoVol: "vol3",                // 関連解説動画（任意）
  videoTitle: "操作解説 Vol.3「ベタ基礎 人通口開口補強と亀甲分割応力算定」",
  tags: ["ベタ基礎", "人通口", "補強筋"]
}
```
